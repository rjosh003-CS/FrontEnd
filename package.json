{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -r node_modules",
    "start": "npx nodemon index.js",
    "dev": "npx nodemon index.js -e js,ejs",
    "build": "npm install && npm run dev",
    "rebuild":"npm run clean && npm run build"
  },
  "author": "Rohit Joshi",
  "license": "MIT",
  "dependencies": {
    "ejs": "^3.1.8",
    "express": "^4.18.2"
  }
}
